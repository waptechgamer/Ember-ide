"""Test package initializer.

This file is intentionally empty but required so that the
``unittest`` discovery can import the ``tests`` package as a module.
Without it the test runner fails with ``ModuleNotFoundError`` when
trying to import ``tests.smoke_test``.
"""
